"use strict"; /*treat all code as newer version of javascript, although it isn't necessary to write this since the code is
usually treated as newer version of js*/

// alert(4+4) we are using node js not browser, so the output won't be 8
 
// bigInt - used for very large number
// string - " "
// null -> standalone value
// undefined - value hasn't been assigned yet
// symbol - used for uniqueness, heavily used by application like figma

//object