// javascript allows comparison of different data types

console.log(2>3)  // it's op is false
console.log("2"<4) // it's op is true

// to avoid the above scenario we use === // === is used for strict check, it not only compares but also checks the data type

console.log("5"===4)